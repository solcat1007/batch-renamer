# BatchRenamer
> 批量重命名剪辑素材文件

## 功能
- 自定义前缀 + 起始序号 + 序号位数 + 步长 + 后缀模板
- 实时预览重命名效果
- 支持撤销操作（两步临时名策略避免冲突）
- 纯 Python 标准库实现，无需额外依赖

## 使用方法
```bash
python batch_renamer.py
```
启动 GUI 后添加待重命名文件，设置命名模板，点击「执行重命名」即可。
